describe('insert a test group description', () => {
    it('insert a test description', async () => {
        const response = await fetch('http://localhost:3000/');
        // TODO: 
    });
});